
/* =========================================================
   File: checkpoint.sql
   ---------------------------------------------------------
   Concept:
   - Forces WAL checkpoint
   - Flushes dirty buffers to disk
   - Ops/debug only
   ========================================================= */

CHECKPOINT;