/* =========================================================
   File: schema/schema.sql
   ---------------------------------------------------------
   Concept:
   - Defines namespaces
   - Separates domains logically
   - Prevents name collision
   ========================================================= */

CREATE SCHEMA IF NOT EXISTS public;
-- CREATE SCHEMA IF NOT EXISTS clinic;
-- CREATE SCHEMA IF NOT EXISTS billing;