**Temporary Mode**

Inside sqlite3, run:

.headers on
.mode table

Now the output will be formatted as a table.

**Permant Mode.**

Create the file (home/user/{file_here})

nano ~/.sqliterc

Inside .sqliterc, paste:

.headers on
.mode table

Save and exit.
